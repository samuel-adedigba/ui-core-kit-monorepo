{
  "name": "ui-core-kit-monorepo",
  "private": true,
  "workspaces": [
    "packages/ui-core",
    "packages/demo"
  ],
  "scripts": {
    "start": "vite",
    "build": "yarn workspace ui-core-kit run build",
    "preview": "vite preview",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces lint -- --fix",
    "prettier": "npx prettier src --check",
    "prettier:fix": "yarn workspaces prettier -- --write",
    "format": "yarn workspaces run format"
  }
}